<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newproject">

    <application
        android:allowBackup="true"
        android:icon="@drawable/peso_do_aluno"
        android:label="@string/app_name"
        android:roundIcon="@drawable/peso_do_aluno"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_resultado_ncd"></activity>
        <activity android:name=".NcdActivity" />
        <activity android:name=".Activity_resultado_imc" />
        <activity android:name=".ImcActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>